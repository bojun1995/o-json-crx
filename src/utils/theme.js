// themepack
// import matrix from '@/style/theme/theme-pack/test.module.scss'

export default () => {
  const chgTheme = (themeName = 'sunlight') => {
    // matrix
    // debugger
    // document.body.style.setProperty('--el-color-primary', '#41b883')
  }
  return {
    chgTheme,
  }
}
